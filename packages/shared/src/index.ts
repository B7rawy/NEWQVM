// Shared contract between api and web. Keep framework-agnostic (no NestJS/React imports).
// DTOs, enums, role constants, and tenant types live here so both sides stay in sync.

export * from "./roles";
